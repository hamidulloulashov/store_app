import 'package:flutter/material.dart';

class SucecWidget extends StatelessWidget {
  const SucecWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        
      ],
    );
  }
}